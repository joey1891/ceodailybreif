export interface Popup {
  id: string;
  title: string;
  content?: string;
  image_url?: string;
  link_url?: string;
  start_date: string;
  end_date: string;
  is_active: boolean;
  created_at: string;
} 